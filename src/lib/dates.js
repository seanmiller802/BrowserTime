import moment from 'moment';

// return first millisecond of previous 60 minutes
export const lastHour = () => {

};

// return first millisecond of current day
export const today = () => {

};

// return millisecond range of previous day
export const yesterday = () => {
  
};

// return millisecond range of previous 7 days
export const lastSeven = () => {
  
};

// return millisecond range of previous 14 days
export const lastFourteen = () => {
  
};

// return millisecond range of previous 30 days
export const lastThirty = () => {
  
};

// return millisecond range for custom start and end
export const custom = (start, end) => {
  
};